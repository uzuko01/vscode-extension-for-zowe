function setListHandler(content) {
  const {list} = content;


}

window.addEventListener('message', event => {
  const {type, content} = event.data;

  switch (type) {
    case "setList":

      break;
  }
});
